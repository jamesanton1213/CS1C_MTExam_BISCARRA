let studentName = prompt("Insert student name:");
let subjectTitle = prompt("Insert subject title:");
let classSchedule = prompt("Insert class schedule (Time, Days):");
let classroom = prompt("Insert classroom:");
let classInstuctor = prompt("Insert class instructor:");

console.log(`${studentName} is currently enrolled in ${subjectTitle} with a class schedule of ${classSchedule} at room ${classroom}.
The instructor for the subject is ${classInstuctor}`);
